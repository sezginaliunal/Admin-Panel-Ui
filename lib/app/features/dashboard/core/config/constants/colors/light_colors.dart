import 'package:flutter/material.dart';

/// Light Mode için renk paleti
/// Soft, modern ve göz yormayan admin panel teması
class LightColors {
  // === PRIMARY COLORS === (Soft Dark Blue-Grey)
  static const primary = Color(0xFF1E293B); // Slate-800
  static const onPrimary = Color(0xFFFFFFFF);
  static const primaryContainer = Color(0xFFE2E8F0); // Slate-200
  static const onPrimaryContainer = Color(0xFF0F172A);

  // === SECONDARY COLORS === (Soft Grey)
  static const secondary = Color(0xFF475569); // Slate-600
  static const onSecondary = Color(0xFFFFFFFF);
  static const secondaryContainer = Color(0xFFF1F5F9);
  static const onSecondaryContainer = Color(0xFF1E293B);

  // === TERTIARY COLORS === (Pastel Blue Accent)
  static const tertiary = Color(0xFF60A5FA); // Blue-400
  static const onTertiary = Color(0xFFFFFFFF);
  static const tertiaryContainer = Color(0xFFEEF6FF);
  static const onTertiaryContainer = Color(0xFF1E3A8A);

  // === BACKGROUND & SURFACE ===
  static const background = Color(0xFFF8FAFC); // very soft grey
  static const onBackground = Color(0xFF0F172A);
  static const surface = Color(0xFFFFFFFF);
  static const onSurface = Color(0xFF0F172A);
  static const surfaceVariant = Color(0xFFF1F5F9);
  static const onSurfaceVariant = Color(0xFF475569);

  // === ERROR COLORS === (Soft Red)
  static const error = Color(0xFFEF4444);
  static const onError = Color(0xFFFFFFFF);
  static const errorContainer = Color(0xFFFEE2E2);
  static const onErrorContainer = Color(0xFF7F1D1D);

  // === SUCCESS COLORS === (Soft Green)
  static const success = Color(0xFF22C55E);
  static const onSuccess = Color(0xFFFFFFFF);
  static const successContainer = Color(0xFFDCFCE7);
  static const onSuccessContainer = Color(0xFF14532D);

  // === WARNING COLORS === (Soft Amber)
  static const warning = Color(0xFFF59E0B);
  static const onWarning = Color(0xFFFFFFFF);
  static const warningContainer = Color(0xFFFEF3C7);
  static const onWarningContainer = Color(0xFF78350F);

  // === INFO COLORS === (Soft Sky Blue)
  static const info = Color(0xFF38BDF8);
  static const onInfo = Color(0xFFFFFFFF);
  static const infoContainer = Color(0xFFE0F2FE);
  static const onInfoContainer = Color(0xFF075985);

  // === OUTLINE & BORDERS ===
  static const outline = Color(0xFFE2E8F0);
  static const outlineVariant = Color(0xFFF1F5F9);
  static const shadow = Color(0x14000000);
  static const scrim = Color(0x33000000);

  // === APP BAR ===
  static const appBarBackground = Color(0xFFFFFFFF);
  static const appBarForeground = Color(0xFF0F172A);

  // === BOTTOM NAVIGATION ===
  static const bottomNavBackground = Color(0xFFFFFFFF);
  static const bottomNavSelected = Color(0xFF1E293B);
  static const bottomNavUnselected = Color(0xFF94A3B8);

  // === CARDS ===
  static const cardBackground = Color(0xFFFFFFFF);
  static const cardShadow = Color(0x08000000);
  static const cardBorder = Color(0xFFE2E8F0);

  // === BUTTONS ===
  static const buttonPrimary = Color(0xFF1E293B);
  static const buttonSecondary = Color(0xFFF1F5F9);
  static const buttonDisabled = Color(0xFFE2E8F0);
  static const onButtonDisabled = Color(0xFF94A3B8);

  // === TEXT FIELDS ===
  static const textFieldBackground = Color(0xFFFFFFFF);
  static const textFieldBorder = Color(0xFFE2E8F0);
  static const textFieldBorderFocused = Color(0xFF60A5FA);
  static const textFieldBorderError = Color(0xFFEF4444);
  static const textFieldHint = Color(0xFF94A3B8);
  static const textFieldText = Color(0xFF0F172A);
  static const textFieldLabel = Color(0xFF475569);

  // === DIVIDERS ===
  static const divider = Color(0xFFF1F5F9);
  static const dividerThick = Color(0xFFE2E8F0);

  // === CHIPS & TAGS ===
  static const chipBackground = Color(0xFFF8FAFC);
  static const chipForeground = Color(0xFF1E293B);
  static const chipBorder = Color(0xFFE2E8F0);

  // === ICONS ===
  static const iconPrimary = Color(0xFF1E293B);
  static const iconSecondary = Color(0xFF64748B);
  static const iconDisabled = Color(0xFFCBD5E1);

  // === OVERLAY & BACKDROP ===
  static const overlay = Color(0x12000000);
  static const backdrop = Color(0x55000000);

  // === STATUS ===
  static const disabled = Color(0xFFE2E8F0);
  static const inactive = Color(0xFFCBD5E1);

  LightColors._();
}
